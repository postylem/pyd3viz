Create D3.js visualizations of python data for use in jupyter notebooks.

Create a subclass of `D3Vizable` for your use case.

Example `ExampleCircles` displays an array of circles. See `d3viz/Example.ipynb`.